# Type Mismatch Example

`format_user` requires an integer, but `handle` passes a string. The expected fix is to convert and validate the input at the boundary or change the function contract consistently.
